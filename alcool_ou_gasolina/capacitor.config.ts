import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'alcool_ou_gasolina',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
